# dependencies
node_modules/

# WXT
.wxt/
.output/
stats.html

# build / test artifacts
dist/
coverage/

# Playwright E2E
test-results/
playwright-report/

# env & secrets (Trakt OAuth creds never committed)
.env
.env.*
!.env.example

# extension signing keys — private .pem never committed; the PUBLIC key is
# embedded directly in wxt.config.ts. Ignore the whole dir at any depth.
.keys/

# editor / OS
.DS_Store
*.local
.idea/
.vscode/*
!.vscode/extensions.json
